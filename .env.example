# WorkOS Configuration
# Get these from your WorkOS Dashboard: https://dashboard.workos.com/
WORKOS_API_KEY="sk_..."
WORKOS_CLIENT_ID="client_..."

# A long, random string used to sign and encrypt the session cookie.
# You can generate one with: openssl rand -base64 32
WORKOS_COOKIE_PASSWORD="..."

# The public URL of your project, used for OAuth redirects.
# For local testing, this can be http://localhost:3000
NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL="http://localhost:3000"